# Public Assets

Only project-owned interface and editor assets remain in this public copy.

GTA data, proprietary fonts, audio, Social Club files, runtime caches, and
prebuilt game assets were removed. Add legally owned copies locally when making
a private runnable APK, and keep them ignored by Git.

Tunnel clients, compiled runtimes, and audio files are also local-only.
